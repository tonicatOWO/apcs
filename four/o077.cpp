#include <bits/stdc++.h>
using namespace std;

int main() {
  int H,W,N,r,c,t,x;
  std::cin >> H >> W >> N;
  int d[H][W];

  while (N--) {
    std::cin >> r >> c >> t >> x;
    for (size_t i = 0; i < length; i++) {
      
    }
  }
}
